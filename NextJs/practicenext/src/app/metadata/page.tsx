import React from 'react'

export const metadata = {
  title: "Page Title",
  description: "This is a specific page description",
};

// Use this to define metadata specific to a single page.
// Ideal for page-specific titles, descriptions, or Open Graph tag

const MetaPage = () => {
  return (
    <div>MetaPage</div>
  )
}

export default MetaPage