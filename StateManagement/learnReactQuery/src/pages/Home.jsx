import { useQuery } from "@tanstack/react-query";
import React from "react";

const Home = () => {

  const {data,isLoading} = useQuery({
    queryKey:
  })
  return <div>Home</div>;
};

export default Home;
