# Currying

- Currying is used to transform a multi-argument object into a sequence of function that can take one argument at a time
- So it is useful for partial applications, react handlers 

# Why do we need currying

- More reusable
- partial application