/* 
An anonymous function is a function without a name.

But this alone will cause an error if it's not assigned or used properly. 
So usually, we assign it to a variable, pass it as an argument, or use it immediately.

*/

function () {
  console.log("I'm anonymous!");
}