/* 
 
== It converts the operands into same type then converts them
=== It checks for same data type and same operands/content. Also known for strict equality check

🔹 == (Loose Equality)
Compares values, but allows type coercion.

JS will try to convert one or both operands to the same type before comparing.

🔹 === (Strict Equality)
Compares both value AND type.

No type conversion — if types differ, it returns false.
*/
